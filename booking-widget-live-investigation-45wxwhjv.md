# Booking Widget Live Investigation

- On 2026-07-24, `https://thestayandwander.com/booking` rendered the prior Booking-page Hotels content: retained hotel fields but no visible Trip.com iframe and no visible Booking-page Flight-tab state.
- The managed deployment domain `https://staywander-bmgf8krv.manus.space/booking` timed out in the browser, so it could not be used to compare the active deployment.
- The local project source contains the intended inline `TripComHotelWidget` and reused `AviasalesFlightWidget`; the next step is to diagnose deployment/custom-domain freshness and make the Booking-page implementation visibly robust.
- Production HTML headers were dynamic and referenced a bundle containing the new widget markers, yet the deployed Booking-page screenshot showed a large blank space beneath the hotel fields rather than a visible Trip.com search interface. This indicates that the application build is current but the third-party frame is not rendering visibly in that production context.
- After allowing the production page to settle, the Trip.com iframe rendered visibly below the retained hotel fields. Switching to the Flights tab showed the reused Aviasales component’s loading state (`Loading flight search...`) rather than the removed static redirect button, confirming the live component is mounted and awaiting its external provider response.
- The development Booking page initially showed a correctly reserved 320px hotel-widget area below the retained fields while the external Trip.com iframe loaded. The shared Aviasales component previously retained its own visible `Loading flight search...` text after the third-party widget mounted; this has been corrected in source so the fallback fades out on the provider script load event.
- After the development iframe settled, the inline Trip.com search widget rendered at its centered 320px maximum width beneath the hotel fields. The temporary `Loading hotel search...` fallback is now limited to the pre-load state and no longer leaves an unexplained blank card area.
- The Booking-page Flights tab was also checked after its third-party script mounted. The live Aviasales search form rendered in the widget area and the visual loading fallback was no longer displayed; text-only extraction may still list the hidden fallback because it remains in the DOM for a graceful opacity transition.
